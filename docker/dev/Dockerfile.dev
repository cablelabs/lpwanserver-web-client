FROM node:latest

WORKDIR /app

COPY /package*.json ./

RUN npm install

ENV HOST=0.0.0.0

EXPOSE 3000

ENV REACT_APP_REST_SERVER_URL=
ENV REACT_APP_PUBLIC_BASE_URL=
ENV REACT_APP_OAUTH_TIMEOUT=

CMD ["npm", "start"]