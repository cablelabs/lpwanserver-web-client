version: '3'

services:
  ui:
    build:
      context: ./../..
      dockerfile: docker/dev/Dockerfile.dev
    image: lpwanserver-web-client-dev
    environment:
      - NODE_ENV=development
    volumes:
      - ../..:/app
    ports:
      - '3000:3000'